interface:
  display_name: "NestJS Code Audit"
  short_description: "Audit a NestJS repository without changing code"
  default_prompt: "Audit the current NestJS repository and return one prioritized, evidence-backed report. Do not modify the project."

policy:
  allow_implicit_invocation: true
