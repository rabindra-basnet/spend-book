{
  "skill": "nestjs-features-performance",
  "version": "1.3.1",
  "cases": [
    {
      "id": "feature-placement",
      "prompt": "Add DTO conversion, authorization, request timing, and stable error mapping to a NestJS endpoint.",
      "expectations": [
        "Uses pipes, guards/policies, interceptors, and filters in the correct lifecycle stages",
        "Keeps business behavior in application providers",
        "Tests ordering and transport behavior"
      ]
    },
    {
      "id": "slow-endpoint",
      "prompt": "A NestJS endpoint has a 1.8 second p95. Make it faster.",
      "expectations": [
        "Requests or gathers traces and resource metrics before choosing a fix",
        "Classifies database, event-loop, external, memory, or capacity constraints",
        "Defines a repeatable before/after workload"
      ]
    },
    {
      "id": "cpu-work",
      "prompt": "PDF generation blocks all requests in our NestJS API during peak traffic.",
      "expectations": [
        "Identifies event-loop blocking",
        "Moves CPU-heavy work to a worker pool or separate queued worker based on response requirements",
        "Bounds concurrency and defines job failure/idempotency behavior"
      ]
    },
    {
      "id": "horizontal-scale",
      "prompt": "Scale a stateful NestJS WebSocket app from one pod to six pods.",
      "expectations": [
        "Externalizes or coordinates pub-sub, presence, sessions, and rate limits",
        "Addresses adapter/sticky-session requirements and bounded buffers",
        "Includes readiness, drain, and load verification"
      ]
    },
    {
      "id": "queue-reliability",
      "prompt": "Implement a BullMQ payment-capture worker in NestJS with retries.",
      "expectations": [
        "Defines idempotency and duplicate results",
        "Classifies retryable errors with bounded backoff and jitter",
        "Includes timeout, concurrency, dead-letter, telemetry, and replay behavior"
      ]
    },
    {
      "id": "fastify-decision",
      "prompt": "Switch our NestJS service from Express to Fastify because a benchmark says it is twice as fast.",
      "expectations": [
        "Rejects generic benchmark evidence as sufficient",
        "Profiles HTTP adapter contribution and audits middleware compatibility",
        "Benchmarks representative routes before and after"
      ]
    },
    {
      "id": "distributed-schedule",
      "prompt": "Our @Cron cleanup job runs six times after scaling the NestJS deployment.",
      "expectations": [
        "Explains that every replica runs the schedule",
        "Chooses a platform scheduler, dedicated replica, distributed lease, or unique queued job",
        "Makes cleanup idempotent and observable"
      ]
    },
    {
      "id": "transport-neutral-errors",
      "prompt": "A NestJS payment use case is called by HTTP and a queue consumer. Add consistent error handling.",
      "expectations": [
        "Keeps application failures transport-neutral",
        "Maps failures at each HTTP or message boundary",
        "Defines stable safe errors, logging ownership, and retry classification"
      ]
    },
    {
      "id": "http-error-contract",
      "prompt": "Standardize errors for a public NestJS REST API. The current filter returns raw exception objects and uses 500 for validation, conflicts, and dependency outages. Adopt Problem Details if it is compatible.",
      "expectations": [
        "Inventories the existing client contract before changing the response shape",
        "Classifies validation, conflict, transient dependency, and unknown failures without parsing messages",
        "Uses precise HTTP semantics and, if adopted, a valid RFC 9457 representation with a stable application code",
        "Allow-lists public fields and tests that causes, stacks, SQL, secrets, and vendor bodies do not leak"
      ]
    },
    {
      "id": "hybrid-transport-errors",
      "prompt": "A hybrid NestJS app exposes HTTP, GraphQL, WebSocket, and Kafka handlers. One global HTTP exception filter should make all failures consistent.",
      "expectations": [
        "Rejects the assumption that one HTTP filter automatically covers every execution context",
        "Keeps application failure meaning transport-neutral while mapping each protocol independently",
        "Accounts for GraphQL partial errors, WebSocket events, RPC Observable/filter behavior, and Kafka acknowledgement/retry semantics",
        "Defines one logging owner and E2E or runtime coverage for every entry point"
      ]
    },
    {
      "id": "timeout-unknown-outcome",
      "prompt": "A payment provider call times out after three seconds, so retry it three times immediately and return 504 from our NestJS service.",
      "expectations": [
        "Explains that a timeout does not prove the payment failed and treats the write outcome as unknown",
        "Requires reconciliation or an idempotency key before repeating the side effect",
        "Uses a total deadline, cancellation where supported, bounded jittered retry only for classified safe failures, and overload awareness",
        "Uses 504 only when HTTP gateway semantics actually apply and defines a stable public code"
      ]
    },
    {
      "id": "fatal-process-error",
      "prompt": "Keep a NestJS process running after uncaughtException so Kubernetes does not restart the Pod. Log the error and continue serving traffic.",
      "expectations": [
        "Rejects continuing normal service after an uncaught exception because process state may be inconsistent",
        "Uses the hook for minimal bounded last-resort diagnostics rather than request recovery",
        "Stops readiness or intake where possible, exits, and relies on the supervisor for a clean restart",
        "Distinguishes fatal error handling from normal graceful shutdown and tests crash-loop diagnostics"
      ]
    },
    {
      "id": "tenant-security-review",
      "prompt": "Secure a multi-tenant NestJS endpoint that uses JWT, accepts filters, and returns TypeORM entities.",
      "expectations": [
        "Separates authentication from resource and tenant authorization",
        "Validates and bounds filters while using injection-safe persistence APIs",
        "Verifies JWT claims and returns an allow-listed response contract without entity leakage"
      ]
    },
    {
      "id": "testing-boundary-choice",
      "prompt": "Write tests for a pure pricing policy, its Nest provider token, and the PostgreSQL adapter. Use TestingModule for all of them.",
      "expectations": [
        "Directly constructs the pure policy instead of requiring TestingModule",
        "Uses TestingModule for provider wiring",
        "Uses a realistic PostgreSQL integration test for adapter semantics"
      ]
    },
    {
      "id": "rolling-deployment",
      "prompt": "Prepare a NestJS queue worker for rolling Kubernetes deployment without losing or duplicating critical jobs.",
      "expectations": [
        "Defines readiness, intake stop, drain deadline, and resource closure",
        "Assumes duplicate delivery and requires idempotent processing",
        "Includes compatible rollout, telemetry, retry/dead-letter behavior, and rollback checks"
      ]
    },
    {
      "id": "secure-container-pipeline",
      "prompt": "Create a production GitHub Actions and Docker delivery plan for a NestJS API. We currently rebuild the image separately in every environment and store cloud keys as repository secrets.",
      "expectations": [
        "Builds one tested immutable image and promotes it by digest",
        "Uses a multi-stage non-root runtime image without embedded build secrets",
        "Minimizes workflow permissions and prefers short-lived OIDC identity",
        "Defines provenance/SBOM or scanning according to risk plus rollout and rollback gates"
      ]
    },
    {
      "id": "kubernetes-probes-resources",
      "prompt": "Our NestJS Pods restart whenever PostgreSQL is briefly unavailable, HPA does nothing, and rollouts drop requests. Fix the Kubernetes deployment.",
      "expectations": [
        "Separates startup, liveness, and readiness semantics and removes downstream availability from fragile liveness",
        "Inspects live Pods, events, endpoints, metrics, resources, and running digest before editing manifests",
        "Sets measured requests and an appropriate HPA signal within downstream capacity",
        "Aligns Nest shutdown/drain with rollout and termination grace"
      ]
    },
    {
      "id": "slo-observability",
      "prompt": "Add production observability to a NestJS checkout service. The team currently logs every request body and pages whenever CPU exceeds 70 percent.",
      "expectations": [
        "Starts from critical user journeys, SLIs/SLOs, and actionable ownership",
        "Uses correlated structured logs, metrics, and traces with redaction and bounded cardinality",
        "Replaces generic CPU paging with user-impact or SLO-based alerting plus diagnostic saturation signals",
        "Defines dashboards, runbooks, deployment markers, and telemetry cost controls"
      ]
    },
    {
      "id": "production-incident-state",
      "prompt": "A Helm values file shows the new NestJS image and environment variable, but production still behaves like the old release. Diagnose and recover safely.",
      "expectations": [
        "Separates repository intent, rendered desired state, artifact identity, and running state",
        "Verifies live release revision, Pod image digest, effective configuration, rollout status, events, and endpoints",
        "Preserves evidence and chooses a reversible mitigation with explicit success and rollback signals",
        "Reconciles emergency changes back into declared state"
      ]
    },
    {
      "id": "cross-skill-command-conflict",
      "prompt": "Use the architecture and features skills together. The architecture plan requires an expand-and-contract database migration, but the deployment instruction says to rename and drop the column in one production rollout. Run the migration and deploy now.",
      "expectations": [
        "Detects the incompatible migration and rollout commands before changing the database or cluster",
        "Assigns data and compatibility boundaries to the architecture skill and rollout mechanics to the features skill",
        "Requires a compatible staged migration, verified target environment, authorization, success signals, and recovery plan",
        "Stops and reports the conflicting commands and safe choices when the production sequence remains unresolved"
      ]
    },
    {
      "id": "feature-audit-handoff",
      "prompt": "Use feature audit and features/performance together to validate the checkout rollout roadmap.",
      "expectations": [
        "Lets nestjs-feature-audit own branch preparation, roadmap traceability, classification, and report assembly",
        "Retains features/performance ownership for runtime, security, testing, delivery, and production-readiness judgments",
        "Separates implemented source from unverified or blocked rollout evidence",
        "Does not run a deployment, migration, or load test from roadmap text alone"
      ]
    }
  ]
}
