interface:
  display_name: "NestJS Professional Software Engineering"
  short_description: "Implement verified, idiomatic NestJS changes"
  default_prompt: "Inspect the current NestJS project, select the clearest supported syntax, implement the requested change, and verify it with the project's checks."

policy:
  allow_implicit_invocation: true
