interface:
  display_name: "NestJS Feature Audit"
  short_description: "Compare one feature with its documented roadmap"
  default_prompt: "Audit one NestJS feature against its documented roadmap on the requested branch and return the required evidence-backed implementation, gap, legacy, bug, and blocker report. Do not implement fixes."

policy:
  allow_implicit_invocation: true
